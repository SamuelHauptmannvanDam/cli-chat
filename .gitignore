node_modules/
mailbox.db
mailbox.db-*
mailbox-server.db
mailbox-server.db-*
# Each user generates their own identity + contacts locally; never ship them.
users/
# Account backups bundle identity.json (private keys) — keep them out of git.
account-backup-*.tgz
.wrangler/
*.log

# build output
dist/
