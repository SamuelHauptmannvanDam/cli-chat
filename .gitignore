node_modules/
mailbox.db
mailbox.db-*
mailbox-server.db
mailbox-server.db-*
# Each user generates their own identity + contacts locally; never ship them.
users/
# Account backups bundle identity.json (private keys) — keep them out of git.
account-backup-*.tgz
.wrangler/
*.log
# Real deploy config holds your own D1 id + worker name — keep it local.
# Self-hosters copy wrangler.toml.example → wrangler.toml and fill in theirs.
wrangler.toml

# build output
dist/
